{
  "version": "1.14.1",
  "notes": "Splat Suite 1.14.1 - Cancel now actually stops COLMAP: the stop flag was only read between lines of output, and the mapper's bundle adjustment is silent for tens of minutes. It also ends the whole process tree (a COLMAP.bat wrapper hid the real exe) and says what survives - the database keeps its features and matches, so a later alignment resumes at mapping. Includes 1.14.0: multi-source solves, trainer recipes, render farm, faster sampling for handheld captures.",
  "url": "https://github.com/userpick05/splatsuite/releases/latest"
}
